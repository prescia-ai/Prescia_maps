DATABASE_URL=postgresql+asyncpg://postgres:password@localhost:5432/aurik
MAPBOX_TOKEN=your_mapbox_token_here
NPS_API_KEY=
# Groq API key for AI-powered Site Insight summaries (optional)
# Leave blank to disable — the /score endpoint will return summary=null gracefully
GROQ_API_KEY=
# Supabase Auth (JWT secret from your Supabase project → Settings → API)
SUPABASE_JWT_SECRET=
SUPABASE_URL=
# Stripe billing — see BILLING_SETUP.md for setup instructions
STRIPE_SECRET_KEY=
STRIPE_PUBLISHABLE_KEY=
STRIPE_WEBHOOK_SECRET=
STRIPE_PRICE_MONTHLY=
STRIPE_PRICE_ANNUAL=
